<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.timeline.vpn.dao.db.DevUseinfoDao">
	<resultMap id="DevUseinfoMap" type="DevUseinfoPo">
		<id column="devId" property="devId" />
		<result column="platform" property="platform" />
		<result column="version" property="version" />
		<result column="creatTime" property="creatTime" />
		<result column="lastUpdate" property="lastUpdate" />
		<result column="userName" property="userName" />
		<result column="longitude" property="longitude" />
		<result column="latitude" property="latitude" />
	</resultMap>
	<insert id="replace">
		replace into
		vpn.devuseinfo(devId,platform,version,creatTime,lastUpdate,userName,latitude,longitude)
		values
		(#{devId},#{platform},#{version},#{creatTime},#{lastUpdate},#{userName},#{latitude},#{longitude})
	</insert>
	<select id="get" resultMap="DevUseinfoMap">
		select * from vpn.devuseinfo where devId=#{devId}
	</select>
	<select id="getAll" resultMap="DevUseinfoMap">
		select * from vpn.devuseinfo
	</select>
	<update id="update">
	    update vpn.devuseinfo 
			<set>
				<if test="lastUpdate != null">
					lastUpdate = #{lastUpdate},
				</if>
				<if test="version != null">
					version = #{version},
				</if> 
				<if test="platform != null">
					platform = #{platform},
				</if> 
				userName = #{userName},
				latitude = #{latitude},
				longitude = #{longitude}
			</set>
			where devId = #{devId}
	</update>
</mapper>
